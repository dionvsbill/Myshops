import {createClient} from '@/lib/supabase/server'; import {ProductCard} from '@/components/ProductCard'; export default async function Wishlist(){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return <div className="container py-12">Sign in to view your wishlist.</div>;const {data}=await s.from('wishlist_items').select('products(*)').eq('user_id',user.id);return <div className="container py-10"><h1 className="mb-6 text-3xl font-black">Wishlist</h1><div className="grid grid-cols-2 gap-4 md:grid-cols-4">{(data||[]).map(x=><ProductCard key={x.products?.id} p={x.products}/>)}</div></div>}