'use client'; import {useState} from 'react'; import {createClient} from '@/lib/supabase/client'; export default function Forgot(){const [email,setEmail]=useState('');const [msg,setMsg]=useState('');const submit=async(e:React.FormEvent)=>{e.preventDefault();const s=createClient();const {error}=await s.auth.resetPasswordForEmail(email,{redirectTo:location.origin+'/reset-password'});setMsg(error?.message||'Check your email for a password reset link.');};return <div className="container max-w-md py-16"><h1 className="text-3xl font-black">Reset password</h1><form onSubmit={submit} className="mt-6 grid gap-4"><input required type="email" value={email} onChange={e=>setEmail(e.target.value)} placeholder="Email" className="rounded-xl border px-4 py-3"/><button className="rounded-xl bg-slate-900 py-3 text-white">Send reset link</button></form>{msg&&<p className="mt-4">{msg}</p>}</div>}