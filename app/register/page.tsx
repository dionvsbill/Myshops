'use client'; import {useState} from 'react'; import {createClient} from '@/lib/supabase/client';
export default function Register(){const [f,setF]=useState({name:'',email:'',password:''});const [msg,setMsg]=useState('');const submit=async(e:React.FormEvent)=>{e.preventDefault();const s=createClient();const {error}=await s.auth.signUp({email:f.email,password:f.password,options:{data:{full_name:f.name},emailRedirectTo:location.origin+'/auth/callback'}});setMsg(error?.message||'Check your email to verify your account.');};return <div className="container max-w-md py-16"><h1 className="mb-6 text-3xl font-black">Create account</h1><form onSubmit={submit} className="grid gap-4"><input required placeholder="Full name" className="rounded-xl border px-4 py-3" value={f.name} onChange={e=>setF({...f,name:e.target.value})}/><input required type="email" placeholder="Email" className="rounded-xl border px-4 py-3" value={f.email} onChange={e=>setF({...f,email:e.target.value})}/><input required minLength={8} type="password" placeholder="Password" className="rounded-xl border px-4 py-3" value={f.password} onChange={e=>setF({...f,password:e.target.value})}/><button className="rounded-xl bg-slate-900 py-3 text-white">Create account</button></form>{msg&&<p className="mt-4">{msg}</p>}</div>}