import {createClient} from '@/lib/supabase/server'; import Link from 'next/link'; import {money} from '@/lib/format';
export default async function Orders(){const s=await createClient();const {data:{user}}=await s.auth.getUser();const {data}=await s.from('orders').select('*').eq('user_id',user!.id).order('created_at',{ascending:false});return <div className="container py-10"><h1 className="mb-6 text-3xl font-black">Orders</h1><div className="space-y-3">{(data||[]).map(o=><Link key={o.id} href={'/account/orders/'+o.id} className="block rounded-2xl border bg-white p-5"><b>{o.order_number}</b><span className="float-right">{money(Number(o.total))}</span><p className="text-sm text-slate-500">{o.status}</p></Link>)}</div></div>}