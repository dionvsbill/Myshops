import {createClient} from '@/lib/supabase/server'; import {ProductCard} from '@/components/ProductCard'; export default async function Products(){const s=await createClient();const {data}=await s.from('products').select('*').eq('is_active',true).order('created_at',{ascending:false});return <div className="container py-8"><h1 className="mb-6 text-3xl font-black">All products</h1><div className="grid grid-cols-2 gap-4 md:grid-cols-4">{(data||[]).map(p=><ProductCard key={p.id} p={p}/>)}</div></div>}