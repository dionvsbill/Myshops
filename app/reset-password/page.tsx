'use client'; import {useState} from 'react'; import {createClient} from '@/lib/supabase/client'; export default function Reset(){const [password,setPassword]=useState('');const [msg,setMsg]=useState('');const submit=async(e:React.FormEvent)=>{e.preventDefault();const s=createClient();const {error}=await s.auth.updateUser({password});setMsg(error?.message||'Password updated.');};return <div className="container max-w-md py-16"><h1 className="text-3xl font-black">Choose a new password</h1><form onSubmit={submit} className="mt-6 grid gap-4"><input required minLength={8} type="password" value={password} onChange={e=>setPassword(e.target.value)} placeholder="New password" className="rounded-xl border px-4 py-3"/><button className="rounded-xl bg-slate-900 py-3 text-white">Update password</button></form>{msg&&<p className="mt-4">{msg}</p>}</div>}