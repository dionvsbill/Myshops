import {NextResponse} from 'next/server'; import {createClient} from '@/lib/supabase/server'; export async function POST(req:Request){const s=await createClient();const {data:{user}}=await s.auth.getUser();if(!user)return NextResponse.json({error:'Authentication required'},{status:401});const {product_id}=await req.json();const {data:existing}=await s.from('wishlist_items').select('id').eq('user_id',user.id).eq('product_id',product_id).maybeSingle();if(existing)await s.from('wishlist_items').delete().eq('id',existing.id);else await s.from('wishlist_items').insert({user_id:user.id,product_id});return NextResponse.json({active:!existing});}