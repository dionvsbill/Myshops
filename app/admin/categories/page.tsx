import {createClient} from '@/lib/supabase/server'; export default async function Categories(){const s=await createClient();const {data}=await s.from('categories').select('*').order('name');return <div className="container py-10"><h1 className="mb-6 text-3xl font-black">Categories</h1><div className="grid gap-3 md:grid-cols-3">{(data||[]).map(c=><div key={c.id} className="rounded-2xl border bg-white p-5"><b>{c.name}</b><p className="text-sm text-slate-500">{c.slug}</p></div>)}</div></div>}