import {createClient} from '@/lib/supabase/server'; import {money} from '@/lib/format'; export default async function AdminOrders(){const s=await createClient();const {data}=await s.from('orders').select('*').order('created_at',{ascending:false});return <div className="container py-10"><h1 className="mb-6 text-3xl font-black">Orders</h1><div className="space-y-3">{(data||[]).map(o=><div key={o.id} className="rounded-2xl border bg-white p-5"><b>{o.order_number}</b><span className="float-right">{money(Number(o.total))}</span><p>{o.status}</p></div>)}</div></div>}