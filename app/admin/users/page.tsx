import {createClient} from '@/lib/supabase/server'; export default async function Users(){const s=await createClient();const {data}=await s.from('profiles').select('id,email,full_name,role,created_at').order('created_at',{ascending:false});return <div className="container py-10"><h1 className="mb-6 text-3xl font-black">Customers</h1><div className="overflow-x-auto rounded-2xl border bg-white"><table className="w-full"><tbody>{(data||[]).map(u=><tr className="border-t" key={u.id}><td className="p-4">{u.full_name}</td><td>{u.email}</td><td>{u.role}</td></tr>)}</tbody></table></div></div>}