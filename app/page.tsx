import Link from 'next/link'; import {createClient} from '@/lib/supabase/server'; import {ProductCard} from '@/components/ProductCard';
export default async function Home(){const s=await createClient();const [{data:featured},{data:products},{data:cats}]=await Promise.all([s.from('products').select('*').eq('is_active',true).eq('is_featured',true).order('created_at',{ascending:false}).limit(8),s.from('products').select('*').eq('is_active',true).order('created_at',{ascending:false}).limit(10),s.from('categories').select('*').order('name')]);return <div className="container py-8 space-y-12"><section className="rounded-3xl bg-slate-900 p-8 text-white md:p-14"><p className="mb-3 text-brand-50">Ghana's everyday online shop</p><h1 className="max-w-2xl text-4xl font-black md:text-6xl">Quality products delivered across Ghana.</h1><p className="mt-4 max-w-xl text-slate-300">Shop electronics, phones, home essentials and more with secure checkout and order tracking.</p><Link href="/products" className="mt-7 inline-block rounded-xl bg-white px-6 py-3 font-bold text-slate-900">Shop products</Link></section><section><h2 className="mb-5 text-2xl font-bold">Categories</h2><div className="grid grid-cols-2 gap-3 md:grid-cols-5">{(cats||[]).map(c=><Link key={c.id} href={'/category/'+c.slug} className="rounded-2xl border bg-white p-5 font-semibold">{c.name}</Link>)}</div></section><section><div className="mb-5 flex items-center justify-between"><h2 className="text-2xl font-bold">Featured</h2><Link href="/products">View all</Link></div><div className="grid grid-cols-2 gap-4 md:grid-cols-4">{(featured?.length?featured:products||[]).map(p=><ProductCard key={p.id} p={p}/>)}</div></section></div>}